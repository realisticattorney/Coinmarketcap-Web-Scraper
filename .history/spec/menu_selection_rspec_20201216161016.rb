require_relative '../lib/menu_selection.rb'

describe MenuSelection do
      it "creates an instance" do
         menu = MenuSelection.new(1)
         expect(menu).to eql()
      end
      it "checks" do
         expect(s)
end
