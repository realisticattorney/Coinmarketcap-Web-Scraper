require 'HTTParty'
require 'Nokogiri'

def Scraper
   url = "https:/blockwork.cc/"
   unparsed_page = HTTParty.get(url) # this does a get request to that URL whick gives back the raw HTML of that page
   parsed_page =                # 
end